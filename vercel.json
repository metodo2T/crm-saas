{
  "framework": "nextjs",
  "buildCommand": "pnpm --filter=web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- apps/web packages/db"
}
